language: objective-c
cache:
  - bundler
  - cocoapods
before_install:
  - pod repo update > /dev/null
install:
  - bundle update
  - bundle install
  - bundle exec pod install
script:
  - fastlane test